/* import { render, screen } from './test-utils';
import '@testing-library/jest-dom';
import App from '../App';

test('renders the app without crashing', () => {
  render(<App />);

  // Check for the presence of a known element
  expect(screen.getByText('Loading...')).toBeInTheDocument();
});
 */